#ifndef CONSTRUCT_H
#define CONSTRUCT_H


#include "stree.h"

// Constructs a suffix tree based on the global variable `text` and populates
// the fields in `st` with values corresponding to the result.
void construct(STree *st);

#endif
